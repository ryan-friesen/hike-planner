import styles from './styles.module.css';

const PlannedHike = (props) => {
  console.log(props);
  return <div className={`${styles.containerBorder}`}>TEST PLANNED HIKE</div>;
};

export default PlannedHike;
